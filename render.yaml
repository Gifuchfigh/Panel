services:
  - type: web
    name: Guard-Panel
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python main.py
    disk:
      name: guard-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DOMAIN
        sync: false
      - key: DB_PATH
        value: /data/panel.db
